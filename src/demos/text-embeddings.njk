---
layout: layouts/base.njk
title: "Demo: sentence embeddings"
description: A quantized sentence-embedding model running entirely in your browser — compare meanings without your text leaving the page.
---

<h1>Sentence embeddings in the browser</h1>

<p>
	A quantized MiniLM sentence-embedding model runs in your browser and
	scores how similar your sentences are in meaning — not spelling. Each
	line below becomes a 384-dimensional vector; pairs are ranked by
	cosine similarity. Your text is embedded on your own machine and never
	sent anywhere.
</p>

<section class="demo" data-demo data-worker="/js/workers/embed-worker.js" data-size="~40 MB">
	<template>
The cat sat on the mat.
A feline rested on the rug.
The stock market fell sharply today.
Equities dropped steeply this afternoon.
I love programming in Python.
	</template>
</section>

<p class="post-meta">
	Runtime: Transformers.js + ONNX Runtime (WebGPU with WASM fallback),
	model all-MiniLM-L6-v2 (q8), all pinned and served from
	<code>/vendor/</code> and <code>/models/</code>. See the
	<a href="/colophon/">colophon</a>.
</p>

<script type="module" src="/js/demo-chrome.js"></script>
